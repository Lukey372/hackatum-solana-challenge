# hackaTUM Solana Challenge – PizzaPay

## Local

Create a `.env` in the root directory and add `USDC_MINT` and `MERCHANT_WALLET` as environment variables.

```
npm run dev
```
and go to `localhost:3000` and `localhost:3000/api/user`
